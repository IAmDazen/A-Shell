this shell is still in development, so as A#, to run, you need python installed, later on it will be converted to EXE file, so here is a list of commands
Def
consoleWIN:
heres a list of things you can define!
pygame.QUIT
heres things you can define AS
windowQuit.event